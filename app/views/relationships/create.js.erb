$("#ff_button<%=@user.id%>").html(' <%= form_for(current_user.active_relationships.find_by(followed_id: @user.id),html: { method: :delete }, remote: true) do |f| %><%= f.submit "Unfollow", class: "btn" %><% end %>');
$("#followers<%=@user.id%>").html('<%= @user.followers.count %>');
